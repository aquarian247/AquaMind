# Analysis / Experimental Migration Scripts

This folder contains exploratory or one-off analysis scripts. These are not part of the supported migration pipeline.

Recent additions:
- `fw_to_sea_deterministic_evidence.py`
  - Deterministic outside-component linkage evidence from stitched members + `SubTransfers` + grouped-organisation destination context.
  - No heuristic matching is used.
